{
  "name": "weather-dashboard",
  "version": "1.0.0",
  "description": "An advanced weather dashboard built with TypeScript, Chart.js, and the free Open-Meteo API.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "python3 -m http.server 8080",
    "start": "npm run build && npm run serve"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
